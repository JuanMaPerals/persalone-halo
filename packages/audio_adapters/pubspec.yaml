name: persalone_audio_adapters
description: Canonical Android-first host audio adapters for PersalOne HORIZON.
version: 0.1.0
publish_to: none
environment:
  sdk: ^3.6.0
resolution: workspace
dependencies:
  flutter:
    sdk: flutter
  persalone_contracts:
    path: ../contracts
dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.1.1
flutter:
  uses-material-design: true
